"""Test package for LittleFuzzyClock.

Exists so `python3 -m unittest discover` from the repo root recurses into
this directory. Tests import the app modules (`fuzzyclock_core`, etc.) from
the repo root, which unittest puts on sys.path as the discovery start dir.
"""
